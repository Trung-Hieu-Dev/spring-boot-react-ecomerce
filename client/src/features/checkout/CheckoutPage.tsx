import React from "react";
import { Typography } from "@mui/material";

const CheckoutPage = () => {
    return (
        <Typography>
            Checkout page for logged-in user only.
        </Typography>
    );
};

export default CheckoutPage;
